<div class="debug">
    Doc:
    {{doc|safe}}

    Doc URL:
    {{doc.url|safe}}

    Doc URL Path:
    {{doc.url.path}}

    Doc Locales:
    {{doc.locales}}

    Env:
    {{env|safe}}

    Fields:
    {% for key, val in doc.fields %}
        {{key}}: {{val}}
    {% endfor %}

    Test plugins:
    {{"Hello World"|testPluginFilter}}, {{"Hello World"|testShortcutFilter}}, {{copyrightYear()}}
</div>