$path: /${doc.collectionPath}/${doc.basename}/
$view: /views/base.njk
$localization:
  path: /intl/${doc.locale.id}/${doc.collectionPath}/${doc.basename}/
  locales:
  - de
  - en
  - fi
  - fr
  - it
  - ja
  - ko
  - nl